/** Mock: valid authCode for device-link verification. */
export const MOCK_VALID_AUTH_CODE = 'tach'

/** POST path (mock — wired to capygo-api later). */
export const API_DEVICE_VERIFY_LINK = '/api/device/verify-link'
